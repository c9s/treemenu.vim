
let interface like this:

    cal TM('Action.Edit.XXX',':command ARGS',{ ... options ... })
    cal TM('Action.Edit.AAA','cal Functions()',{ ... options ... })

write a script to translate , from:

    Action.Edit.XXX         :command ARGS { ... options ... }
    Action.Undo             :Undo
    Action.Redo             :Redo

